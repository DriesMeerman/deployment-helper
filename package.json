{
  "name": "deployment-helper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && parcel build src/* --target node --out-dir build && cp -r config/ build/"
  },
  "keywords": [
    "deployment",
    "webhooks"
  ],
  "author": "Dries Meerman",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "express-rate-limit": "^5.1.1",
    "parcel": "^1.12.4"
  }
}
